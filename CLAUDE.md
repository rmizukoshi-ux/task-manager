# Claude ワークフロー定義

## メール Triage

以下の手順でメールtriageを実行する。

### 実行手順

1. **ラベル・カテゴリ確認**
   - `list_labels` で利用可能なラベルを確認する（必要に応じて）

2. **未読メールの確認**（全受信トレイ）
   - `search_threads` で `is:unread in:inbox` を検索し、未読メールの全体像を把握する

3. **TO宛て未読メールへの返信下書き作成**
   - `search_threads` で `to:me is:unread in:inbox -in:draft` を検索する
   - ヒットした各スレッドについて `get_thread` で本文を取得する
   - 以下の条件を満たすメールに対して、返信文章を作成し `create_draft` で下書き保存する
     - 自動配信メール・no-replyアドレスからのメールは**スキップ**する
     - 返信不要と明記されているメールは**スキップ**する
   - 返信文は、メール本文の内容を踏まえて自然な日本語（または相手の言語）で作成する
   - `create_draft` の `replyToMessageId` に元メッセージIDを設定してスレッド返信にする

4. **完了報告**
   - 処理したメール数、下書き作成数、スキップ数をまとめて報告する
